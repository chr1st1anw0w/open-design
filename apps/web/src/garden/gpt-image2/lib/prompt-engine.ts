export function renderPrompt(template: string, args: Record<string, string>): string | { error: string } {
  return template;
}
export function extractPlaceholders(template: string): string[] {
  return [];
}
