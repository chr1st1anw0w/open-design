#!/usr/bin/env sh
set -eu

remote_name="${1:-}"
remote_url="${2:-}"

case "$remote_url" in
  *nexu-io/open-design* )
    echo "[BLOCKED] Push to upstream is not allowed: $remote_name -> $remote_url" >&2
    exit 1
    ;;
esac

exit 0
